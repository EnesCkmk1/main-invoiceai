{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "express",
  "installCommand": "npm ci --include=dev && npm --prefix server ci --include=dev && npm --prefix client ci --include=dev",
  "buildCommand": "npm --prefix server run prisma:generate && npm --prefix client run build && rm -rf public && mkdir -p public && cp -r client/dist/* public/",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
