{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "express",
  "installCommand": "npm --prefix server ci && npm --prefix client ci",
  "buildCommand": "npm --prefix server run prisma:generate && npm --prefix client run build && rm -rf server/public && mkdir -p server/public && cp -r client/dist/* server/public/",
  "outputDirectory": "server/public",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
