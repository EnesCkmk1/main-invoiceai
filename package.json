{
  "name": "invoiceflow-ai",
  "version": "1.0.0",
  "private": true,
  "description": "InvoiceFlow AI — create and send professional invoices in under 30 seconds",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run prisma:generate && npm --prefix server run build && npm --prefix client run build",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck"
  },
  "engines": {
    "node": "22.x"
  },
  "type": "module",
  "dependencies": {
    "express": "^5.2.1"
  }
}
